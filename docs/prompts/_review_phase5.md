> ⚠️ **DEPRECATED v1** — 이 보고서의 "축약 권장" 항목 다수가 잘못된 방향이었음.
> 메인 서사의 디렉션 구체성이 AI 편향 억제 장치임을 간과한 검토.
> 정정된 검토는 [_review_v2_corrected.md](_review_v2_corrected.md) 참조.

---

# Phase 5 검토 — 오디션 11파일

## 분류
- `오디션모드_EN.json`: 오디션 메타 룰 (영문 압축)
- 라운드 6파일: 1R / 2R / 3R-A/B/C / 4R
- 막간 4파일: 초반 / 중반 / 합숙(메타) / 후반

---

## 🟢 잘 작성된 부분 (모범 사례)

### 1. **오디션모드_EN.json — 영문 압축 잘됨**
- `perspective` 3분기 (producer/contestant/shared) 명확
- `contestant_specifics` 분리로 모드 전환 시 충돌 없음
- `tags` 시스템: `🎙️1→🤝2→🏠0→⚔️3→🥊3→🏆3→👑4` — 진행 단계 시각화
- `round_flow.transitions` 4개: 라운드 전환 MC 대사 + 태그 교체 룰

### 2. **막간_초반/중반/후반 분리 — UX 잘 살림**
- 합숙 1개월 동안 시기별 분위기 다르게 활성
- events/tone/user_role 3필드 일관
- 트리거가 시기별 키워드로 명확히 구분 (입소/방배정 → 미션/연습실 → 대항전/전야)

### 3. **라운드 6파일 — 매우 압축적 (영문, 평균 300~500자)**
- flow / branching / end 핵심 구조
- 4R의 private/public 분리는 정교 (오프카메라 + 온카메라)
- epilogue: "Produce·Prime·Priority → Produce·Prism·Priority" 언어유희 — 좋은 디테일

---

## 🔴 분리 가능 (UX 영향 적음)

### 1. **막간_합숙_EN.json (메타)** vs 막간_초반/중반/후반 (구체)
- 막간_합숙은 "post-2R, 6 enter dorm. ~1 month" 메타만 있음
- 실제 막간 컨텐츠는 초반/중반/후반에 분리됨
- → **막간_합숙은 오디션모드 본체에 통합 가능** → 1파일 절약 (~250자)

### 2. **오디션모드_EN.json의 `slots` 필드**
- 8명 참가자 캐릭터별 1줄 역할 (장그루: Retry/Desperation 등)
- 메인 프롬프트 chars 섹션과 일부 중복
- → 메인 프롬프트의 chars `none` 카테고리에 통합 가능

### 3. **라운드 6파일의 `id` 필드**
- 1, 2, 4, 5, 6, 7번. 의미 불명 (순서일 뿐)
- → 제거 가능

---

## 🟡 모드/SVG 구체화 권장

### 1. **막간_초반에서 SVG 활용 부재**
- "프로듀서 라운지", "참가자 인터뷰 타임" 이벤트
- → SVG_메신저, SVG_커뮤니티 등을 활용하면 더 풍부 (현재 SVG 호출 가이드 없음)

### 2. **3R-B 패자부활** — 1v1 매치 디테일 부족
- "2nd 1v1→1 survives. 3rd 1v1→1 survives." 단순 명시
- → 1v1 매치 시나리오 (선곡 / 포지션 / 심사 기준)이 라운드 자체 분량에 맞지 않음

### 3. **4R 최종선택의 private 단계** — 매우 정교하나 더 활용 가능
- "Contestants quote {{user}}'s actual prior inputs" — 좋은 메커니즘
- 그러나 어떤 시나리오에서 어떤 인용이 가장 강력한지 가이드 부재

### 4. **오디션 시작 시나리오 부재**
- 오프닝_EN.json은 일반 오프닝 (한소리 사무실 → PPP 소개 → 초대장)
- 오디션 시작 시나리오 (1R 첫 무대)은 1R_등급평가의 flow에 압축적으로 있을 뿐
- → 매니저모드_시작 같은 별도 시작 시나리오가 오디션에는 없음

---

## 🔴 차라리 빼는 것이 도움

### 1. **slots 필드의 "장그루: Retry/Desperation" 류 압축**
- 너무 짧아서 정보 가치 없음 (메인 chars로 알 수 있음)
- → 제거

### 2. **timeline "1R:1d→1wk gap→2R:2d→dorm ~1mo→3R:2d→2wk gap→4R:1d. Total ~2 months."**
- 메타 정보. 실제 진행은 막간 로어북이 시기별로 처리
- → 제거 가능 (또는 막간_합숙으로 이동)

### 3. **judges 섹션 (오디션모드 본체)**
- "user / jin / erika" 3줄 — 메인 프롬프트 chars + 캐릭터 본체와 중복
- → 제거 가능

---

## 🔵 토큰 효율화

### 1. **라운드 6파일의 `flow` 필드 → 마크다운 리스트로 압축 가능**
- 현재: "① MC opening+judge intro+entrance → ② Individual stages → ③ ..."
- → 가능. 이미 충분히 압축됨

### 2. **오디션모드_EN.json의 `contestant_specifics`**
- contestant 모드는 별도 옵트인 (!오디션참가)
- → **별도 로어북 분리** 가능 (오디션모드_참가자 EN.json)
- 절약: ~600자 (contestant 모드 미사용 시 활성 안 함)

### 3. **막간 3파일 (초반/중반/후반)의 user_role 중복**
- 모두 "{{user}}는 ... 시기"로 시작
- 표현 압축 가능 (~50자/파일 × 3 = 150자)

---

## 종합 권장사항 (Phase 5)

### 우선순위 1: 즉시 가능
1. **막간_합숙 → 오디션모드 본체 통합** (1파일 감소)
2. **오디션모드의 `slots`, `judges`, `timeline` 제거** — 다른 곳에 있음 (~300자)
3. **id 필드 일괄 제거** (~100자)

### 우선순위 2: 구조 개선
4. **contestant_specifics → 별도 로어북** (~600자, 옵트인 활성)
5. **막간 3파일에 SVG 호출 가이드 추가**
6. **4R private 인용 메커니즘 가이드 추가**

### 우선순위 3: 컨텐츠 보강
7. **3R-B 1v1 매치 시나리오 디테일**
8. **오디션 시작 시나리오 분리** (1R 첫 무대 전용)

---

## Phase 5 추정 효과
- 즉시 가능: **약 1,000자 절약** + 1파일 감소
- 구조 개선 후: **약 600자 추가 절약** (옵트인 효과)
- 컨텐츠 보강 시: 1~2,000자 추가 (가치 ↑)
